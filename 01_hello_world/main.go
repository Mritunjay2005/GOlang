package main 
import "fmt"

func main() {
	fmt.Println("hello world")
	//to run this code we have two ways --here we are using main.go as a name 
	// of the file but you can use any name you want but it should be same as 
	// the name of the file you are using to run the code
	//                                1."go run main.go"
	//                                2."go build main.go" then followed by "./main"
	// note here when wwe write main.go we have to use the full address but science we open this terminal in this foldert we can execute directly using file name
	
}